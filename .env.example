# Local dev: leave empty — Vite proxy forwards /api to the API server
# Coolify BUILD variables (required at build time, not runtime):
# VITE_API_URL=https://api-turbbi.maktechgroup.tech/api
# VITE_SITE_URL=https://turbbi.maktechgroup.tech
